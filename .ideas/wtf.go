

// metahandler - ?! Why brain

// ConfigServer?
// OPcode = modify Info,  
func ( *) ConfigServer(s *Server) (error)  {
	switch s.ServerType {
	case 10: // DefaultWebServer
		// By s.ServerID, OPcode?
	default:
		if s.ServerType < 10 {
		// Debug ServerType value
		}

		// Invalid server type
	}
}